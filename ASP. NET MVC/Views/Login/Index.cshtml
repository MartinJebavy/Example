@model Realitni_Kancelar.Models.Person

@{
    ViewBag.Title = "Index";
}


@using (Html.BeginForm())
{
    <div class="form-horizontal">
        
        <div class="form-group">
            <div class="col-md-offset-3 col-md-12">
                <br /><br />
                    <input id="Submit1" type="button" class="btn btn-default" value="Přihlásit" onclick="location.href='@Url.Action("Login", "Login")'" />
                    <input id="Submit2" type="button" class="btn btn-default" value="Registrovat" onclick="location.href='@Url.Action("Register", "Login")'" />

</div>
        </div>
        @Html.ValidationSummary()

    </div>
}
